# Dependencies / build output
node_modules/
build/
dist/
*.egg-info/

# Lockfile
package-lock.json

# Minified bundles + sourcemaps (never hand-format these)
*.min.js
*.min.js.map

# Dash-generated component outputs - regenerated by `npm run build`; formatting them
# is pure churn (the next build overwrites it). The only hand-maintained file in here
# is __init__.py (Python; Prettier doesn't touch it anyway), so ignore the whole dir.
dash_week_range_picker/
